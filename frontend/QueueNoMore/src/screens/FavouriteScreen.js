import React, {Component} from 'react';
import {
  SafeAreaView,
  StyleSheet,
  ScrollView,
  View,
  Text,
  Image,
  TouchableOpacity,
} from 'react-native';

class FvouriteScreen extends Component {
  render() {
    return (
      <SafeAreaView>
        <ScrollView>
          <View>
            <Text>Favourite Screen</Text>
          </View>
        </ScrollView>
      </SafeAreaView>
    );
  }
}

const styles = StyleSheet.create({});

export default FvouriteScreen;
